#!/usr/bin/env sh
set -e

if command -v mvn >/dev/null 2>&1; then
  exec mvn "$@"
fi

echo "Apache Maven is required to run this project. Install Maven or replace this lightweight script with the official Maven Wrapper." >&2
exit 1

